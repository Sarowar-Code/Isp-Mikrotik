#### Register admin

### ui updates
> reusable registration form for admin, reseller, pppclient,
> do check their models.

### fetch and api calls
> backend is separate, check axios api settings, in lib files,
> do admin regitration. in frontend,
> most probably form component will be client side.

